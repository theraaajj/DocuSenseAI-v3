# DocuSenseAI-v2
A local-first, offline RAG designed to query sensitive doc and local directories without leaking data. Implements "Lazy Discovery" to query local files without full-disk indexing and uses schema-aware ingestion for messy Excel/CSV data. This approach keeps the memory footprint low &amp; ensures the system respects user privacy boundaries.... v2 Alpha. 
